import styled from 'styled-components';

export const Instargram = styled.div`
  display: flex;
  img {
    width: 10.7rem;
    height: 10.7rem;
    margin: 0.35rem;
    border-radius: 0.4rem;
    cursor: pointer;
  }
`;
