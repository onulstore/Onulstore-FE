import styled from 'styled-components';

export const Layout = styled.div`
  min-height: 81.2rem;
  max-width: 60rem;
  margin: 0 auto;
  width: 37.5rem;
  position: relative;

  @media (min-width: 1140px) {
    margin: auto;
  }
`;
